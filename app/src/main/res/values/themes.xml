<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        No action bar: with the edge-to-edge that targetSdk 35 forces, the bar
        was drawn over the top of the content instead of above it. The screens
        here are simple enough to carry their own heading and handle their own
        window insets.
    -->
    <style name="Theme.BilingualKeyboard" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
